version: "3.6"

services:
  portainer:
    container_name: "portainer_new"
    image: "portainer/portainer-ce:latest"
    restart: always
    security_opt:
      - no-new-privileges:true

    entrypoint:
      - "/portainer"

    environment:
      - "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

    hostname: "6652bdad9213"

    ipc: "private"

    #labels:
    #  - "traefik.enable=true"
    #  - "traefik.http.routers.portainer.entrypoints=http"
    #  - "traefik.http.routers.portainer.rule=Host(`portainer.local.bigserverenergy.com`)"
    #  - "traefik.http.middlewares.portainer-https-redirect.redirectscheme.scheme=https"
    #  - "traefik.http.routers.portainer.middlewares=portainer-https-redirect"
    #  - "traefik.http.routers.portainer-secure.entrypoints=https"
    #  - "traefik.http.routers.portainer-secure.rule=Host(`portainer.local.bigserverenergy.com`)"
    #  - "traefik.http.routers.portainer-secure.tls=true"
    #  - "traefik.http.routers.portainer-secure.service=portainer"
    #  - "traefik.http.services.portainer.loadbalancer.server.port=9443"
    #  - "traefik.docker.network=proxy"

    logging:
      driver: "json-file"
      options: {}

    mac_address: "02:42:ac:11:00:04"

    #network_mode: "bridge"
    networks:
      - proxy

    ports:
      - "8000:8000/tcp"
      - "9443:9443/tcp"

    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "portainer_data:/data"

    working_dir: "/"

networks:
  proxy:
    external: true

volumes:
  portainer_data:
    external: true